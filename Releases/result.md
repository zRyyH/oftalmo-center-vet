## `array[3243]` of objects (11 keys)

## Object (11 keys)

- **id_r**: `string` — e.g. '300982841'
- **data**: `string` — e.g. '15/10'
- **descricao**: `string` — e.g. 'Água/saerp'
- **fornecedor**: `string` — e.g. 'SAERP'
- **parcela**: `string` — e.g. '...'
- **valor**: `float` — e.g. -366.5
- **tipo**: `string` — e.g. 'despesa'
- **status**: `string` — e.g. 'V'
- **origem**: `string` — e.g. ''
- **vencimento**: `string` — e.g. '15/10/2025'
- **forma_pagamento**: `string` — e.g. ''

---

## Resumo da Estrutura

```
id  →  integer
data  →  string
descricao  →  string
fornecedor  →  string
parcela  →  string
valor  →  float
tipo  →  string
status  →  string
origem  →  string
vencimento  →  string
forma_pagamento  →  string
```
